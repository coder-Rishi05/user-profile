<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Profile</title>
    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-black min-h-screen flex items-center justify-center">

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout"
                class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block hover:bg-red-600 transition">
                Logout
            </a>
        </div>


        <h5 class="mb-5">you can edit your post.</h5>

        <form action="/update/<%= post._id %>" method="POST" class="flex flex-col max-w-lg">
            <textarea name="content" placeholder="What's on your mind?"
                class="resize-none bg-transparent border-2 border-zinc-800 rounded-md w-full p-3 mb-3 outline-none text-white placeholder-gray-400"
                rows="5" required>
            <%= post.content %>
            </textarea>

            <input type="submit" value="update Post"
                class="px-4 py-2 bg-yellow-500 text-white text-sm rounded-md w-40 cursor-pointer hover:bg-blue-600 transition" />
        </form>
    </div>
</body>

</html>